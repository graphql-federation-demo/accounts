name: Node.js PR CI

on:
  pull_request:
    branches: [ "*" ]

env:
 TARGET_BRANCH_NAME: ${{ github.base_ref }} 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: ls
        run: ls -lR;

      - name: Install Inigo CLI
        run: rm -R inigo | curl -s -L https://github.com/inigolabs/artifacts/releases/latest/download/cli_linux_amd64.tar.gz | tar -xz;

      - name: Inigo CLI Login
        env:
          INIGO_USERNAME: ${{ secrets.INIGO_USERNAME }}
          INIGO_PASSWORD: ${{ secrets.INIGO_PASSWORD }}
        run: ./inigo login

      - name: Inigo Check for ${{ env.TARGET_BRANCH_NAME }}
        run: ./inigo check ./inigo/*.yaml --label ${TARGET_BRANCH_NAME}
          
      - run: npm run build
